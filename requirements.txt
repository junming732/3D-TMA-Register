# --- Core Data Handling ---
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
tifffile>=2023.0.0
imagecodecs>=2023.0.0  # Crucial for reading QPTIFF compression

# --- Image Processing & Registration ---
SimpleITK>=2.3.0       # The industry standard for Medical/Bio rigid & deformable registration
scikit-image>=0.21.0   # General filtering (Gaussian, CLAHE, etc.)
opencv-python-headless # Fast geometry/computer vision tools (Headless = server safe)

# --- Visualization & Plotting ---
matplotlib>=3.7.0      # For your "scratch" animations and static plots
tqdm                   # Progress bars (Very useful when processing 20 slices)
ipykernel              # To run Python inside VS Code cells/notebooks

# --- Efficiency ---
zarr                   # Faster format for 3D chunks than TIFF
dask                   # For handling images larger than RAM